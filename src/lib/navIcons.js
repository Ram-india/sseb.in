import {
  BriefcaseIcon,
  BuildingIcon,
  CheckCircleIcon,
  CompassIcon,
  FileEditIcon,
  GearIcon,
  GraduationIcon,
  ListIcon,
  PulseIcon,
  ShieldCheckIcon,
  UserIcon,
  UsersIcon,
} from '../components/Icons'

// Shared by the mega menu, the mobile menu and the section sidebars, so a
// nav entry shows the same glyph everywhere it appears.
export const navIcons = {
  building: BuildingIcon,
  compass: CompassIcon,
  user: UserIcon,
  shield: ShieldCheckIcon,
  graduation: GraduationIcon,
  list: ListIcon,
  check: CheckCircleIcon,
  pulse: PulseIcon,
  gear: GearIcon,
  briefcase: BriefcaseIcon,
  users: UsersIcon,
  file: FileEditIcon,
}
